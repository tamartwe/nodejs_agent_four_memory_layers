# Optional. Without it, act0 falls back to the deterministic ScriptedModel (offline,
# reproducible, free) and says so loudly. Set it to make act0 hit the real API.
ANTHROPIC_API_KEY=
MODEL=claude-haiku-4-5
