# Optional. Without it, every act runs against the deterministic ScriptedModel,
# which is the recommended mode for live demos (offline, reproducible, free).
ANTHROPIC_API_KEY=
MODEL=claude-sonnet-4-6
